export const metadata = {
  title: "Demo",
  description: "Explore EasyCash with sample data — no account required.",
};

export default function DemoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
